# Dev Diary — 2026-04-16

---

## Session 10 — Cloudflare proxy rollout, DNS-01 cert issuance

### What we worked on

A corporate laptop couldn't reach `marie.lostriver.llc` — "site can't be reached." Diagnosed as corp SSL-inspection proxy sending TCP RST during TLS handshake to DigitalOcean IPs (origin 174.138.33.106). Fixed by putting all `lostriver.llc` subdomains behind Cloudflare's orange-cloud proxy. Also moved Traefik cert issuance to DNS-01 via the Cloudflare API so certs keep renewing correctly behind the proxy and are decoupled from inbound port 80.

### Root cause

From the corp laptop:
- DNS resolved fine (corp resolver returned `174.138.33.106`)
- `nc -vz marie.lostriver.llc 443` succeeded (TCP reached origin)
- `curl -v` died mid-TLS with `SSL_ERROR_SYSCALL` right after Client Hello
- Same error with a bogus SNI directed at the IP → rules out domain-specific SNI filtering
- Classic signature of SSL-inspection proxy rejecting connections based on destination IP category (IaaS / uncategorized)

Server was healthy the whole time (external curl: HTTP 200, valid LE cert).

### What got done

**Diagnosis and plan**
- Inventoried all 5 public domains on isidora: `marie`, `newsintel`, `intel`, `analytics`, `vault` (all under `lostriver.llc`) plus `langfuse.local`
- Decided on universal rollout (not per-domain hack) — CF proxy is the "professional" default anyway

**Cloudflare setup**
- Created scoped API token: `Zone:Read` + `DNS:Edit` on `lostriver.llc` only (`traefik-dns01-isidora`)
- Stored token in isidora `/home/deploy/infra/.env` as `CF_DNS_API_TOKEN`, chmod 600
- Set zone SSL/TLS mode → Full (strict), Always Use HTTPS → on (dashboard clicks — token doesn't have Zone Settings:Edit by design)

**Traefik — dual resolver during transition**
- Added `letsencrypt-dns` ACME resolver using Cloudflare DNS provider, alongside existing HTTP-01 `letsencrypt`. Storage split: `/letsencrypt/acme.json` (HTTP-01) vs `/letsencrypt/acme-dns.json` (DNS-01)
- `forwardedHeaders.trustedIPs` on both entrypoints, via `TRUSTED_PROXY_IPS` env (CF IPv4 + IPv6 ranges)
- DNS-01 challenge resolvers pinned to `1.1.1.1,1.0.0.1` (avoid corporate DNS dependency at cert time)
- Both resolvers loaded cleanly; existing certs unaffected

**Per-domain flip** (all via CF API, DNS:Edit scope — `PATCH /dns_records/{id}` with `{"proxied":true}`)
- `analytics.lostriver.llc` → proxied ✓ (pilot)
- `marie.lostriver.llc` → proxied ✓
- `newsintel.lostriver.llc` → proxied ✓
- `intel.lostriver.llc` → proxied ✓
- `vault.lostriver.llc` → proxied ✓ (WebSockets on `/notifications/hub` verified working through CF)

All 5 verified returning HTTP 200 via CF edge (`cf-ray: *-EWR`/`*-IAD`, `server: cloudflare`, cert issuer = Google Trust Services).

**Project compose updates** (each router's `certresolver=letsencrypt` → `letsencrypt-dns`)
- marie, newsintel, UPS_company_intel, vault edited on server
- Only newsintel committed+pushed this session (`git@github.com:jordimo/newsintel.git`, now redirects to `NewsIntel`)
- marie skipped — concurrent editing session
- UPS_company_intel and vault: not git-tracked on server (see Open threads)

**Infra repo changes** (committed in this session)
- `docker-compose.yml` — dual resolver, trustedIPs, CF_DNS_API_TOKEN env wiring
- `.env.example` — documented `CF_DNS_API_TOKEN`, `TRUSTED_PROXY_IPS` with full CF range list
- `init-project.sh` — fixed latent bug (`[ "$TARGET" = "do" ]` never fired; should be `[[ "$TARGET" = do:* ]]`); DNS reminder now prescribes orange-cloud as the convention
- `README.md` — DNS section documents proxied-by-default, DNS-01 cert flow explanation

### Decisions made

- **Keep both resolvers during transition**: acme-dns.json is currently empty because Traefik is still serving valid HTTP-01 certs from acme.json (expiring 2026-07-14). The switch to DNS-01 is prospective — next renewal will exercise the path. Safer than forcing re-issuance now.
- **No CF cache rule for `/api/*`**: verified all 3 API domains return `cf-cache-status: DYNAMIC` by default (CF's built-in logic doesn't cache non-static extensions with no cache headers). Revisit if caching issues appear.
- **DNS-01 over HTTP-01** because: (a) survives origin port-80 restrictions, (b) works cleanly with CF proxy, (c) prereq for future "firewall :443 to CF IPs only" hardening.
- **Orange cloud is now the default convention** for new projects. Documented in README + init-project.sh.

### Issues encountered

- **CF API token scope mismatch**: scoped token can flip proxy (DNS:Edit) but can't change Zone Settings (SSL mode, Always-HTTPS) or add Cache Rules. Left as dashboard actions rather than broadening the token.
- **Local DNS cache masking verification**: first curl to `marie.lostriver.llc` after flipping orange cloud still hit origin IP. Had to force fresh lookup via `dig @1.1.1.1`. Not a bug, just a cache gotcha.
- **Stale `vagent-tunnel.lostriver.llc` entry in acme.json**: Traefik keeps trying to renew it, fails with NXDOMAIN. Harmless noise. Will clean up when moving away from HTTP-01 resolver entirely.
- **Stale caitie-api file-provider healthcheck**: Traefik logs spam about `caitie-api:3000` health check failing (container's been gone for days). Remove from `dynamic/dynamic.yml` in a follow-up.

### Open threads

1. **marie compose update** — needs to land in `jordimo/Marie` repo after user's current editing session wraps. Check the resolver line survives.
2. **UPS_company_intel + vault not git-tracked on server** — compose changes there are only as durable as the filesystem. Should clone these from GitHub properly (if repos exist) or initialize them as repos. `deploy.sh` is non-functional on both until this is fixed.
3. **Remove HTTP-01 resolver** once all 5 certs have renewed via DNS-01 (i.e. sometime after ~2026-06-15). At that point also clean up `acme.json` and the `http-challenge.entrypoint` flag.
4. **Restrict origin :443 to CF IP ranges** (DO firewall or ufw) — final hardening step. Tracked as task #17.
5. **Vaultwarden repo proper naming** — directory is `vault/`, container `vaultwarden`, domain `vault.lostriver.llc`. Consistent but worth noting.
6. **TRUSTED_PROXY_IPS needs periodic refresh** — CF publishes the list at `cloudflare.com/ips-v4` and `ips-v6`. Either manual refresh quarterly or a small cron that pulls and restarts Traefik on diff.
